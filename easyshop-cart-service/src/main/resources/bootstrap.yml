spring:
  application:
    name: easyshop-cart-service  # имя должно совпадать с названием конфиг-файла в репозитории
  cloud:
    config:
      uri: http://localhost:8081  # адрес config-server
      username: config-user       # если настроена безопасность
      password: config-password   # если настроена безопасность
