spring:
  application:
    name: easyshop-order-service  # имя должно совпадать с названием конфиг-файла в репозитории
  cloud:
    config:
      uri: http://localhost:8082  # адрес config-server
      username: config-user       # если настроена безопасность
      password: config-password   # если настроена безопасность
